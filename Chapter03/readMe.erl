% to start type erl
% to stop type q() or init:stop(). everyting will be stopped in systematic way.
% to immediately stop erlang:halt().
% things that start with hypen are annotations -module(). -export().
% numbers with base <base>#<number>.

% arithmatic ops return floating point
% for interger result use functions div, rem
% floating point precision is 64bit.
% single quote and double quote are different.
% 'a' = a % an atom. but "a"  will be string.
%   Erlang  is  a  functional  programming  language,every expression must have a value

% tuples {1,2} 
% use atoms as first value in tuple to represent what the tuple is made of

